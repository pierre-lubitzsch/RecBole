# UV package manager configuration only
# Package installation uses setup.py

[tool.uv]
find-links = ["https://data.pyg.org/whl/torch-2.4.0+cu121.html"]

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu121"
explicit = true

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"

[tool.uv.sources]
torch = { index = "pytorch" }
torchvision = { index = "pytorch" }
torchaudio = { index = "pytorch" }
